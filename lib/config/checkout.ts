// Kill switch para la demo con el cliente: deja el botón de pago visible e
// interactivo (misma validación, mismo estilo) pero no llega a redirigir a
// MercadoPago. Para volver a habilitar el pago real, cambiar a `false`.
export const CHECKOUT_DISABLED = true
